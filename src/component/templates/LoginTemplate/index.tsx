import LoginSection from "@/component/organism/LoginSection";

const LoginTemplate = () => {
  return (
    <main className="min-h-screen flex items-center justify-center bg-gray-100">
      <LoginSection />
    </main>
  );
};

export default LoginTemplate;
